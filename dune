;(rule
  ;(targets c_flags.sexp c_library_flags.sexp camlp5.cmd)
;  (targets camlp5.cmd)
;  (deps    config/discover.exe)
;  (action  (run config/discover.exe ))
;)



(rule
  (targets logger-flags.cfg)
  (deps    config/discover.exe)
  (action  (run config/discover.exe -logger-flags))
)
